let:
    requirements = [req(name="language/python")]
    runtime = solve(
        requirements=requirements,
        platforms=["12345", "67890"]
    )

    testing = solve(requirements=append(requirements, req(name="testing")))
in:
    runtime
