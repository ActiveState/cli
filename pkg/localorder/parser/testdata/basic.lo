let:
  # TODO: Add support for solve_legacy function identifier
  runtime = solve(
    platforms = ["linux", "windows"],
    requirements = [req(name="requests"), req(name="language/python")]
  )
in: 
  runtime